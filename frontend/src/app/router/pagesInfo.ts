export enum Pages {
    MAIN_LAYOUT = 'main-layout',
    FEED = 'feed',
    USER_PROFILE_REDIRECT_TO_CURRENT = 'user-profile-redirect-to-current',
    USER_PROFILE = 'user-profile',
    DIRECT = 'direct',
    DIRECT_LAYOUT = 'direct_layout',
    LOG_IN = 'log-in',
    SIGN_UP = 'sign-up',
    NOT_FOUND = 'not-found',
}

export const authPages = ['log-in', 'sign-up']